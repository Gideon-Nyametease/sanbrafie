<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-darkk col-12" >
  <div class="container-fluid" id="navbar-example">
    <a class="navbar-brand" href="/">
      <img src="<%= asset_pack_path 'media/images/logo.png' %>" height="60px" width="100" alt=" demo instructions">
    </a>
    <p style="font-family:  'Montserrat'!important; font-size: 16px; font-weight: 500 ; color: #fdfdfd;">Sanbrafie Group Inc</p>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if !user_signed_in? %>
          <li class="nav-item active">
            <a class="nav-link my-routes active" href="/">HOME</a>
          </li>
          <li class="nav-item " id="destination-btn">
            <a class="nav-link my-routes" href="#destinations" >TRIPS</a>
          </li>
          <li class="nav-item">
            <a class="nav-link my-routes" href="#testimonials">TESTIMONIALS</a>
          </li>
          <li class="nav-item">
            <a class="nav-link my-routes" href="#about">ABOUT US</a>
          </li>
          <li class="nav-item">
            <a class="nav-link my-routes" href="#contact">CONTACT US</a>
          </li>
        <% else %>
          <%# <li class="nav-item">
            <a class="nav-link active my-routes" href="/tours">Tours</a>
          </li> %>
        <% end %>
      </ul>
      <div class="d-flex">
        <% if user_signed_in? %>
          <%= link_to "Logout", destroy_user_session_path , :method=> 'delete',class: "btn btn-lg btn-outline-light nav-buttons me-auto"  %>
        <% else %>
          <%= link_to "Login", new_user_session_path ,class: "btn btn-lg btn-outline-light nav-buttons me-auto"  %>
          <%= link_to "Sign up", new_user_registration_path, class:"btn btn-lg btn-light nav-buttons" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
<style>
  .bg-darkk{
    background-color: #090909 !important;
  }
  .active-border{
    border-bottom: 2px solid #202020;
  }
  .my-routes{
    font-family:  'Montserrat'!important;
    font-weight: 400 !important;
    font-size: 14px !important;
  }
</style>
<script>
  var scrollSpy = new bootstrap.ScrollSpy(document.body, {
    target: '#navbar-example'
  })
  console.log("00000000")
 
</script>
